//to be put inside the common.js
function serverURL(){
	return " http://bit-mp.biz/m03";
}



//php file to be put into the server for mysql 
<?php
$servername = "localhost";
$username = "seetoh88_m03";
$password = "uefari12";
$dbname = "seetoh88_m03";

try {
    $conn = new PDO("mysql:host=$servername;dbname=$dbname", $username, $password);
    // set the PDO error mode to exception
    $conn->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);

    // prepare sql and bind parameters
    $stmt = $conn->prepare("INSERT INTO testdb (name, password) 
    VALUES (:name, :password)");
    $stmt->bindParam(':name', $name);
    $stmt->bindParam(':password', $password);
    

    // insert a row
    $name = "SMI";
    $password = "Doe";
    $stmt->execute();

  
    echo "name:$name<br> password:$password";
    }
catch(PDOException $e)
    {
    echo "Error: " . $e->getMessage();
    }
$conn = null;
?>


